// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/workflow-engine/workflow-engine/internal/data/ent/predicate"
	"github.com/workflow-engine/workflow-engine/internal/data/ent/processevent"
)

// ProcessEventQuery is the builder for querying ProcessEvent entities.
type ProcessEventQuery struct {
	config
	ctx        *QueryContext
	order      []processevent.OrderOption
	inters     []Interceptor
	predicates []predicate.ProcessEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProcessEventQuery builder.
func (peq *ProcessEventQuery) Where(ps ...predicate.ProcessEvent) *ProcessEventQuery {
	peq.predicates = append(peq.predicates, ps...)
	return peq
}

// Limit the number of records to be returned by this query.
func (peq *ProcessEventQuery) Limit(limit int) *ProcessEventQuery {
	peq.ctx.Limit = &limit
	return peq
}

// Offset to start from.
func (peq *ProcessEventQuery) Offset(offset int) *ProcessEventQuery {
	peq.ctx.Offset = &offset
	return peq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (peq *ProcessEventQuery) Unique(unique bool) *ProcessEventQuery {
	peq.ctx.Unique = &unique
	return peq
}

// Order specifies how the records should be ordered.
func (peq *ProcessEventQuery) Order(o ...processevent.OrderOption) *ProcessEventQuery {
	peq.order = append(peq.order, o...)
	return peq
}

// First returns the first ProcessEvent entity from the query.
// Returns a *NotFoundError when no ProcessEvent was found.
func (peq *ProcessEventQuery) First(ctx context.Context) (*ProcessEvent, error) {
	nodes, err := peq.Limit(1).All(setContextOp(ctx, peq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{processevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (peq *ProcessEventQuery) FirstX(ctx context.Context) *ProcessEvent {
	node, err := peq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProcessEvent ID from the query.
// Returns a *NotFoundError when no ProcessEvent ID was found.
func (peq *ProcessEventQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = peq.Limit(1).IDs(setContextOp(ctx, peq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{processevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (peq *ProcessEventQuery) FirstIDX(ctx context.Context) int64 {
	id, err := peq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProcessEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProcessEvent entity is found.
// Returns a *NotFoundError when no ProcessEvent entities are found.
func (peq *ProcessEventQuery) Only(ctx context.Context) (*ProcessEvent, error) {
	nodes, err := peq.Limit(2).All(setContextOp(ctx, peq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{processevent.Label}
	default:
		return nil, &NotSingularError{processevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (peq *ProcessEventQuery) OnlyX(ctx context.Context) *ProcessEvent {
	node, err := peq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProcessEvent ID in the query.
// Returns a *NotSingularError when more than one ProcessEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (peq *ProcessEventQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = peq.Limit(2).IDs(setContextOp(ctx, peq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{processevent.Label}
	default:
		err = &NotSingularError{processevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (peq *ProcessEventQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := peq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProcessEvents.
func (peq *ProcessEventQuery) All(ctx context.Context) ([]*ProcessEvent, error) {
	ctx = setContextOp(ctx, peq.ctx, ent.OpQueryAll)
	if err := peq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProcessEvent, *ProcessEventQuery]()
	return withInterceptors[[]*ProcessEvent](ctx, peq, qr, peq.inters)
}

// AllX is like All, but panics if an error occurs.
func (peq *ProcessEventQuery) AllX(ctx context.Context) []*ProcessEvent {
	nodes, err := peq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProcessEvent IDs.
func (peq *ProcessEventQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if peq.ctx.Unique == nil && peq.path != nil {
		peq.Unique(true)
	}
	ctx = setContextOp(ctx, peq.ctx, ent.OpQueryIDs)
	if err = peq.Select(processevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (peq *ProcessEventQuery) IDsX(ctx context.Context) []int64 {
	ids, err := peq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (peq *ProcessEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, peq.ctx, ent.OpQueryCount)
	if err := peq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, peq, querierCount[*ProcessEventQuery](), peq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (peq *ProcessEventQuery) CountX(ctx context.Context) int {
	count, err := peq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (peq *ProcessEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, peq.ctx, ent.OpQueryExist)
	switch _, err := peq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (peq *ProcessEventQuery) ExistX(ctx context.Context) bool {
	exist, err := peq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProcessEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (peq *ProcessEventQuery) Clone() *ProcessEventQuery {
	if peq == nil {
		return nil
	}
	return &ProcessEventQuery{
		config:     peq.config,
		ctx:        peq.ctx.Clone(),
		order:      append([]processevent.OrderOption{}, peq.order...),
		inters:     append([]Interceptor{}, peq.inters...),
		predicates: append([]predicate.ProcessEvent{}, peq.predicates...),
		// clone intermediate query.
		sql:  peq.sql.Clone(),
		path: peq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EventType string `json:"event_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProcessEvent.Query().
//		GroupBy(processevent.FieldEventType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (peq *ProcessEventQuery) GroupBy(field string, fields ...string) *ProcessEventGroupBy {
	peq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProcessEventGroupBy{build: peq}
	grbuild.flds = &peq.ctx.Fields
	grbuild.label = processevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EventType string `json:"event_type,omitempty"`
//	}
//
//	client.ProcessEvent.Query().
//		Select(processevent.FieldEventType).
//		Scan(ctx, &v)
func (peq *ProcessEventQuery) Select(fields ...string) *ProcessEventSelect {
	peq.ctx.Fields = append(peq.ctx.Fields, fields...)
	sbuild := &ProcessEventSelect{ProcessEventQuery: peq}
	sbuild.label = processevent.Label
	sbuild.flds, sbuild.scan = &peq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProcessEventSelect configured with the given aggregations.
func (peq *ProcessEventQuery) Aggregate(fns ...AggregateFunc) *ProcessEventSelect {
	return peq.Select().Aggregate(fns...)
}

func (peq *ProcessEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range peq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, peq); err != nil {
				return err
			}
		}
	}
	for _, f := range peq.ctx.Fields {
		if !processevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if peq.path != nil {
		prev, err := peq.path(ctx)
		if err != nil {
			return err
		}
		peq.sql = prev
	}
	return nil
}

func (peq *ProcessEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProcessEvent, error) {
	var (
		nodes = []*ProcessEvent{}
		_spec = peq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProcessEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProcessEvent{config: peq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, peq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (peq *ProcessEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := peq.querySpec()
	_spec.Node.Columns = peq.ctx.Fields
	if len(peq.ctx.Fields) > 0 {
		_spec.Unique = peq.ctx.Unique != nil && *peq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, peq.driver, _spec)
}

func (peq *ProcessEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(processevent.Table, processevent.Columns, sqlgraph.NewFieldSpec(processevent.FieldID, field.TypeInt64))
	_spec.From = peq.sql
	if unique := peq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if peq.path != nil {
		_spec.Unique = true
	}
	if fields := peq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, processevent.FieldID)
		for i := range fields {
			if fields[i] != processevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := peq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := peq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := peq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := peq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (peq *ProcessEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(peq.driver.Dialect())
	t1 := builder.Table(processevent.Table)
	columns := peq.ctx.Fields
	if len(columns) == 0 {
		columns = processevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if peq.sql != nil {
		selector = peq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if peq.ctx.Unique != nil && *peq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range peq.predicates {
		p(selector)
	}
	for _, p := range peq.order {
		p(selector)
	}
	if offset := peq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := peq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProcessEventGroupBy is the group-by builder for ProcessEvent entities.
type ProcessEventGroupBy struct {
	selector
	build *ProcessEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pegb *ProcessEventGroupBy) Aggregate(fns ...AggregateFunc) *ProcessEventGroupBy {
	pegb.fns = append(pegb.fns, fns...)
	return pegb
}

// Scan applies the selector query and scans the result into the given value.
func (pegb *ProcessEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pegb.build.ctx, ent.OpQueryGroupBy)
	if err := pegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProcessEventQuery, *ProcessEventGroupBy](ctx, pegb.build, pegb, pegb.build.inters, v)
}

func (pegb *ProcessEventGroupBy) sqlScan(ctx context.Context, root *ProcessEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pegb.fns))
	for _, fn := range pegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pegb.flds)+len(pegb.fns))
		for _, f := range *pegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProcessEventSelect is the builder for selecting fields of ProcessEvent entities.
type ProcessEventSelect struct {
	*ProcessEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pes *ProcessEventSelect) Aggregate(fns ...AggregateFunc) *ProcessEventSelect {
	pes.fns = append(pes.fns, fns...)
	return pes
}

// Scan applies the selector query and scans the result into the given value.
func (pes *ProcessEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pes.ctx, ent.OpQuerySelect)
	if err := pes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProcessEventQuery, *ProcessEventSelect](ctx, pes.ProcessEventQuery, pes, pes.inters, v)
}

func (pes *ProcessEventSelect) sqlScan(ctx context.Context, root *ProcessEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pes.fns))
	for _, fn := range pes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
